[project]
name = "canvas-grades"
version = "0.1.0"
description = "Forecast Canvas grades using API data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "canvasapi>=3.5.0",
    "python-dotenv>=1.2.2",
]
